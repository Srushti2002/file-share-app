PORT=5000
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
JWT_SECRET=change_me
# Comma-separated list of allowed frontend origins for CORS
# Example: http://localhost:5173,https://your-frontend.example.com
CLIENT_ORIGINS=http://localhost:5173
MAX_FILE_SIZE_MB=20

# Public base URL of this API (no trailing slash). Used to build absolute share links.
# Example: https://your-backend.example.com
API_BASE_URL=https://file-share-app-zy7k.onrender.com

# S3 removed: all files are stored and shared locally
